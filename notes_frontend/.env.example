# Base URL for the notes backend REST API (used by src/lib/apiClient.ts)
# Example (local dev): http://localhost:3001
NEXT_PUBLIC_NOTES_API_BASE=

# Optional: public site URL for auth redirect flows (if/when backend auth is implemented)
NEXT_PUBLIC_SITE_URL=
